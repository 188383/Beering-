<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/craft_beer">
    <Button
        android:id="@+id/list_beers"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/add_beer"
        android:text="@string/list_beers"/>
    <Button
        android:id="@+id/add_beer"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/location"/>
    <Button
        android:id="@+id/friends"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/add_beer"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="@string/friend_options"/>
    <Button
        android:id="@+id/about"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/friends"
        android:layout_centerHorizontal="true"
        android:text="@string/about"/>
</RelativeLayout>